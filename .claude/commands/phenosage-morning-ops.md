---
description: Daily ops briefing — CI health, deploy state, stale PRs, dependency staleness, and any action items.
---

Run the `phenosage-ops-commander` skill. Produce the briefing in the exact format specified in `.claude/skills/phenosage-ops-commander/SKILL.md`.

After the briefing:

- If there are action items, suggest which to tackle first based on severity.
- If nothing is red, end with a one-line "green across the board" and stop.
